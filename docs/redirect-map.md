# Car Transport With Personal Items Redirect Map

## Current Source Of Truth

- `vercel.json`

## Current Redirect Estate

The current machine-readable deployment contract already includes:

1. host normalization from `www` to apex
2. security headers
3. contact alias handling into the quoting contact endpoint when that local work is committed

## Current Reading

This repo already uses `vercel.json` as the natural place for redirect and header policy. Keep using that file as the operational source of truth when redirect rules evolve.
